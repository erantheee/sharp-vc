interface:
  display_name: "Sharp VC"
  short_description: "像路演答辩一样拷问产品价值，并讲清为什么还不OK"
  default_prompt: "Use $sharp-vc as a strict investment gatekeeper. Judge only the next concrete investment, explain the business reason when evidence is insufficient, update the judgment when new evidence arrives, and ask one decisive question at a time in natural language."
policy:
  allow_implicit_invocation: true
